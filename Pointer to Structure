#include<stdio.h>
struct Student{
    int id;
    char name[50];
};
int main() {
    struct Student s1;
    struct Student *ptr=&s1;
    scanf("%d",&ptr->id);
    scanf("%s", ptr->name);
    printf("ID=%d Name=%s", ptr->id, ptr->name);
    return 0;
}

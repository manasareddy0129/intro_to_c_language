#include<stdio.h>
struct Student {
    int id;
    char name[50];
};
int main() {
    struct Student s[2];
    struct Student *ptr=s;
    for(int i=0;i<2;i++) scanf("%d %s",&ptr[i].id,ptr[i].name);
    for(int i=0;i<2;i++) printf("%d %s\n", ptr[i].id, ptr[i].name);
    return 0;
}

#include<stdio.h>
int main() {
    FILE *fp=fopen("file.txt","a");
    char str[100];
    fgets(str,sizeof(str),stdin);
    fputs(str,fp);
    fclose(fp);
    printf("Data appended successfully");
    return 0;
}

#include<stdio.h>
#include<string.h>
int main() {
    char str[100], sub[50];
    fgets(str, sizeof(str), stdin);
    scanf("%s", sub);
    if(strstr(str, sub)!=NULL) printf("Found");
    else printf("Not Found");
    return 0;
}

#include<stdio.h>
int main() {
    char str[100], oldc, newc;
    fgets(str, sizeof(str), stdin);
    scanf(" %c %c",&oldc,&newc);
    int i=0;
    while(str[i]!='\0') {
        if(str[i]==oldc) str[i]=newc;
        i++;
    }
    printf("%s", str);
    return 0;
}

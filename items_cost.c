#include<stdio.h>
int main()
{
int no_of_items = 30;
int cost_of_each_item = 20;
int total_cost_of_items = no_of_items * cost_of_each_item;
printf("Number of items is %d\n",no_of_items);
printf("Cost of each item is %d\n",cost_of_each_item);
printf("cost of all items is %d",total_cost_of_items);
return 0;
}

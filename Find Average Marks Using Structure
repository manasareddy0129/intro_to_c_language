#include<stdio.h>
struct Student { char name[50]; float marks; };
int main() {
    struct Student s[3];
    float sum=0;
    for(int i=0;i<3;i++) scanf("%s %f", s[i].name,&s[i].marks);
    for(int i=0;i<3;i++) sum+=s[i].marks;
    printf("%.2f", sum/3);
    return 0;
}

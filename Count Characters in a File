#include<stdio.h>
int main() {
    FILE *fp=fopen("file.txt","r");
    int count=0;
    char ch;
    while((ch=fgetc(fp))!=EOF) count++;
    fclose(fp);
    printf("Characters=%d", count);
    return 0;
}

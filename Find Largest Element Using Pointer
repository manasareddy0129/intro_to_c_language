#include<stdio.h>
int main() {
    int n;
    scanf("%d",&n);
    int a[n], *p=a;
    for(int i=0;i<n;i++) scanf("%d",&a[i]);
    int largest=*p;
    for(int i=1;i<n;i++) if(*(p+i)>largest) largest=*(p+i);
    printf("%d", largest);
    return 0;
}

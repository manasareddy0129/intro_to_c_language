#include<stdio.h>
int power(int base,int exp){
    int p=1;
    for(int i=1;i<=exp;i++) p*=base;
    return p;
}
int main() {
    int a,b;
    scanf("%d %d",&a,&b);
    printf("%d", power(a,b));
    return 0;
}

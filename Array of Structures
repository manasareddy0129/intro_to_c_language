#include<stdio.h>
struct Student {
    int id;
    char name[50];
};
int main() {
    struct Student s[3];
    for(int i=0;i<3;i++) scanf("%d %s",&s[i].id,s[i].name);
    for(int i=0;i<3;i++) printf("%d %s\n", s[i].id, s[i].name);
    return 0;
}

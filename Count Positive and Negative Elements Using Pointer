#include<stdio.h>
int main() {
    int n, pos=0, neg=0;
    scanf("%d",&n);
    int a[n], *p=a;
    for(int i=0;i<n;i++) scanf("%d",&a[i]);
    for(int i=0;i<n;i++) {
        if(*(p+i)>0) pos++;
        else if(*(p+i)<0) neg++;
    }
    printf("Positive=%d Negative=%d", pos, neg);
    return 0;
}

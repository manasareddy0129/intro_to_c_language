#include<stdio.h>
int main() {
    int n;
    scanf("%d",&n);
    int a[n], *p=a;
    for(int i=0;i<n;i++) scanf("%d",&a[i]);
    int smallest=*p;
    for(int i=1;i<n;i++) if(*(p+i)<smallest) smallest=*(p+i);
    printf("%d", smallest);
    return 0;
}
